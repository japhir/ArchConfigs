Report extremely concisely; sacrifice grammar for concision.
Give the why behind every design choice; push back when you disagree. Ask which source is normative rather than inferring conventions from git history.
Work on `main`; a branch/worktree only when sessions run in parallel.
Git & subagents: before landing a branch, spawning a subagent, or working in a shared tree, read `~/.claude/docs/git-workflow.md`. Editing `.org`: read `~/.claude/docs/org-mode.md`.
Points I didn't address are undecided. Leave them until one becomes load-bearing, then ask.
